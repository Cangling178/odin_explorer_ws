# racer_localization

[English](README.md) | 简体中文

连续车体状态与全局对齐。

## 状态

计划：选择车轮/ODIN1 输入、协方差策略、单一 TF 发布归属和复位处理。不把任何 EKF 配置宣称为已验证。

## 职责与验收

任务编号：LOC-001, CAL-001。参见根目录的架构与接口文档。
本包目前通过 ament_cmake 安装资源/文档。构建成功不代表规划中的子系统已经可以运行。

## 配置

所有 `*.template.yaml` 都是规格表，不是运行中的 ROS 参数文件。实现组件时再添加运行依赖、可执行程序和经过测试的参数。厂商代码和大型录制数据不放在本包中。

## 当前实现 — 2026-09-16

独立定位节点仍在规划中。当前仿真地图对齐实现在 `lap_controller` 内，轮式里程计来自仿真差速控制器。

入口、参数与验收见[competition lap](../../../simulation/COMPETITION_LAP_cn.md).
