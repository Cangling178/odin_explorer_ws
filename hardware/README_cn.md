# 硬件记录

[English](README.md) | 简体中文

从 `bom.csv` 和 `robot_spec.template.yaml` 开始。后轮差速结构和 F4 下位机已确认；选驱动器前，先明确电机、编码器和具体开发板参数。`platform_lock.template.yaml` 记录后续软件/固件基线。`calibration/` 存放实测标定版本，`electrical/` 存放接线和供电记录，`mechanical/` 存放尺寸与安装资料。

所有未知字段都特意保留，没有任何一个是可直接用于驱动实车的默认值。
